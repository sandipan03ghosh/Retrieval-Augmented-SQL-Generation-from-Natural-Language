import React from 'react';
import { motion } from 'framer-motion';
import { Box, Paper, Card, Typography, Button, IconButton, Container } from '@mui/material';

// Motion-wrapped Material-UI components for animation support
export const MotionBox = motion(Box);
export const MotionPaper = motion(Paper);
export const MotionCard = motion(Card);
export const MotionTypography = motion(Typography);
export const MotionButton = motion(Button);
export const MotionContainer = motion(Container);
export const MotionIconButton = motion(IconButton);
